# 前端登录界面开发计划

## 目标
为"风迹集"项目创建前端登录/注册页面

## 技术栈
- React 18 + Vite
- React Router v6
- Ant Design 5
- Axios
- CSS Modules

## 文件结构
```
frontend/
├── src/
│   ├── pages/
│   │   └── Login/
│   │       ├── Login.tsx          # 登录页面
│   │       ├── Register.tsx       # 注册页面
│   │       └── Login.module.css   # 样式
│   ├── components/
│   │   └── LoginForm/
│   │       └── LoginForm.tsx      # 登录表单组件
│   ├── services/
│   │   └── auth.ts                # 认证 API 服务
│   ├── router/
│   │   └── index.tsx              # 路由配置
│   ├── stores/
│   │   └── authStore.ts           # 认证状态管理 (Zustand)
│   └── App.tsx
```

## 登录页面功能

### 1. 登录表单 (LoginForm.tsx)
- 邮箱输入框 (email)
- 密码输入框 (password)
- 记住登录状态
- 登录按钮
- 表单验证
- 错误提示

### 2. 登录页面 (Login.tsx)
- 左右分栏布局
- 左侧：品牌展示区（Logo + 标语）
- 右侧：登录表单
- 切换到注册页面链接

### 3. 注册页面 (Register.tsx)
- 用户名输入框
- 邮箱输入框
- 密码输入框
- 确认密码输入框
- 注册按钮
- 切换到登录页面链接

### 4. 认证服务 (auth.ts)
- login(email, password)
- register(username, email, password)
- logout()
- getCurrentUser()

### 5. 状态管理 (authStore.ts)
- user 状态
- token 状态
- isAuthenticated
- login/logout 方法

## 验证步骤
1. 启动前端开发服务器 `npm run dev`
2. 访问 /login 页面
3. 测试表单验证（空表单提交）
4. 测试登录成功/失败情况
