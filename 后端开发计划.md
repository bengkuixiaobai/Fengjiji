# 后端架子开发计划

## 目标
为"风迹集"项目创建后端架子，包含登录注册 API

## 技术栈
| 类别 | 技术选型 | 说明 |
|------|----------|------|
| 运行时 | Node.js 18+ | 生态丰富，与前端统一语言 |
| 框架 | Express.js 4.x | 简洁灵活，中间件丰富 |
| 数据库 | PostgreSQL 15 | 关系型，功能强大 |
| ORM | Prisma 5 | 类型安全，迁移方便 |
| 认证 | JWT (jsonwebtoken) | 无状态认证 |
| 密码加密 | bcrypt | 业界标准，不可逆加密 |
| 参数验证 | Joi | 请求参数校验 |

## 数据库选择说明

本项目选用 **PostgreSQL**，原因：
- Prisma 对 PostgreSQL 支持更好
- 数据类型丰富（内置 JSON 支持）
- 适合复杂查询
- 未来扩展性强

> 对比：如果您更熟悉 MySQL 或追求简单，MySQL 也是不错的选择。

## 文件结构
```
backend/
├── src/
│   ├── app.js                 # Express 应用入口
│   ├── server.js               # 服务器启动
│   ├── config/
│   │   └── index.js            # 配置（环境变量）
│   ├── routes/
│   │   ├── index.js            # 路由汇总
│   │   ├── auth.routes.js      # 认证路由
│   │   └── user.routes.js      # 用户路由
│   ├── controllers/
│   │   ├── auth.controller.js  # 认证控制器
│   │   └── user.controller.js  # 用户控制器
│   ├── services/
│   │   ├── auth.service.js     # 认证服务
│   │   └── user.service.js     # 用户服务
│   ├── middleware/
│   │   ├── auth.middleware.js  # JWT 认证中间件
│   │   └── error.middleware.js # 错误处理中间件
│   ├── utils/
│   │   └── response.js         # 统一响应格式
│   └── prisma/
│       └── schema.prisma       # Prisma 数据模型
├── prisma/
│   └── schema.prisma           # 数据库 schema
├── .env.example                # 环境变量示例
├── package.json
└── index.js                   # 入口文件
```

## 核心功能

### 1. 数据库设计 (Prisma Schema)

**User 模型**
| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | Int | PK, AUTO | 自增主键 |
| username | String | UNIQUE, NOT NULL | 用户名 |
| email | String | UNIQUE, NOT NULL | 邮箱 |
| password | String | NOT NULL | 加密后密码 |
| nickname | String? | - | 昵称 |
| avatar | String? | - | 头像 URL |
| bio | String? | - | 个人简介 |
| role | String | DEFAULT 'admin' | 角色 |
| createdAt | DateTime | - | 创建时间 |
| updatedAt | DateTime | - | 更新时间 |

### 2. 认证 API

| 方法 | 路径 | 说明 | 请求体 |
|------|------|------|--------|
| POST | /api/auth/register | 用户注册 | {username, email, password} |
| POST | /api/auth/login | 用户登录 | {email, password} |
| POST | /api/auth/logout | 登出 | - |
| POST | /api/auth/refresh | 刷新 Token | {refreshToken} |

**注册响应**
```json
{
  "success": true,
  "data": {
    "user": { "id": 1, "username": "...", "email": "..." },
    "token": "eyJhbGciOiJIUzI1NiIs...",
    "refreshToken": "eyJhbGciOiJIUzI1NiIs..."
  }
}
```

**登录响应**
```json
{
  "success": true,
  "data": {
    "user": { "id": 1, "username": "...", "email": "..." },
    "token": "eyJhbGciOiJIUzI1NiIs...",
    "refreshToken": "eyJhbGciOiJIUzI1NiIs..."
  }
}
```

### 3. 用户 API

| 方法 | 路径 | 说明 | 请求体 |
|------|------|------|--------|
| GET | /api/users/me | 获取当前用户 | - |
| PUT | /api/users/me | 更新个人信息 | {nickname, avatar, bio} |
| PUT | /api/users/me/password | 修改密码 | {oldPassword, newPassword} |

### 4. 中间件

**JWT 认证中间件**
- 验证 Token 有效性
- 提取用户信息到 req.user
- 未授权返回 401

**错误处理中间件**
- 统一错误响应格式
- 区分客户端错误和服务器错误
- 日志记录

**参数验证中间件**
- 使用 Joi schema 验证
- 验证失败返回 400

### 5. 数据库迁移

```bash
# 安装依赖后
npx prisma migrate dev --name init

# 生成 Prisma Client
npx prisma generate
```

## 启动步骤

```bash
# 1. 进入后端目录
cd backend

# 2. 安装依赖
npm install

# 3. 复制环境变量配置
cp .env.example .env
# 编辑 .env，配置数据库连接

# 4. 运行数据库迁移
npx prisma migrate dev --name init

# 5. 启动开发服务器
npm run dev

# 服务器运行在 http://localhost:3000
```

## 验证 API

```bash
# 注册
curl -X POST http://localhost:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"test","email":"test@example.com","password":"123456"}'

# 登录
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"123456"}'
```

## 后续扩展

完成架子后，可按需添加：
- 博客管理 API (CRUD)
- 分类/标签 API
- 项目管理 API
- 点赞/互动 API
